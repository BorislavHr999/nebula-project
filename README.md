🌌 **Project Nebula**: A full-stack microservices cluster built with Docker Compose.
🚦 **Architecture**: Nginx Reverse Proxy routing traffic to Python (Flask) and Node.js APIs.
⚡ **Data & State**: Redis backend for real-time hit counting and persistent state.
🕹️ **Dashboard**: Interactive UI to visualize load balancing and container responses.
🚀 **Quick Start**: Run `docker compose up -d --build` and visit `http://localhost`.
